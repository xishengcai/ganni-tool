# apply test, not found, then create new
apiVersion: v1
kind: Service
metadata:
  labels:
    app: launcher-svc-new
  name: launcher-svc-new
  namespace: launcher-test
spec:
  ports:
    - port: 90
      protocol: TCP
      targetPort: 80
      name: xxx
  selector:
    app: launcher
---
### 测试不代命名空间
apiVersion: v1
kind: Service
metadata:
  labels:
    xi: xisheng
  name: default-nginx
  namespace: default
spec:
  ports:
    - name: http
      nodePort: 32640
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/instance: default-nginx
    app.kubernetes.io/name: nginx
  type: LoadBalancer