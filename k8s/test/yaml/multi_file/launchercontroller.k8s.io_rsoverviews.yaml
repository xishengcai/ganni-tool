
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.0
    "api-approved.kubernetes.io": "https://github.com/kubernetes/kubernetes/pull/78458"
  creationTimestamp: null
  name: rsoverviews.launchercontroller.k8s.io
spec:
  group: launchercontroller.k8s.io
  names:
    kind: RsOverView
    listKind: RsOverViewList
    plural: rsoverviews
    singular: rsoverview
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RsOverView is a cluster scope object.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RsOverViewSpec is the spec for a Foo resource
            properties:
              deploymentName:
                type: string
              replicas:
                format: int32
                type: integer
              resStatus:
                items:
                  properties:
                    available:
                      description: Total number of available pods (ready for at least
                        minReadySeconds) targeted by this deployment. AvailableReplicas
                        int32 `json:"availableReplicas,omitempty" protobuf:"varint,4,opt,name=availableReplicas"`
                      format: int32
                      type: integer
                    ready:
                      description: Total number of ready pods targeted by this deployment.
                        ReadyReplicas int32 `json:"readyReplicas,omitempty" protobuf:"varint,7,opt,name=readyReplicas"`
                      format: int32
                      type: integer
                    total:
                      format: int32
                      type: integer
                    type:
                      type: string
                    unavailable:
                      description: Total number of unavailable pods targeted by this
                        deployment. This is the total number of pods that are still
                        required for the deployment to have 100% available capacity.
                        They may either be pods that are running but not yet available
                        or pods that still have not been created. UnavailableReplicas
                        int32 `json:"unavailableReplicas,omitempty" protobuf:"varint,5,opt,name=unavailableReplicas"`
                      format: int32
                      type: integer
                  required:
                  - type
                  type: object
                type: array
            required:
            - deploymentName
            - replicas
            - resStatus
            type: object
          status:
            description: RsOverViewStatus is the status for a Foo resource
            properties:
              availableReplicas:
                format: int32
                type: integer
            required:
            - availableReplicas
            type: object
        required:
        - spec
        - status
        type: object
    served: true
    storage: true