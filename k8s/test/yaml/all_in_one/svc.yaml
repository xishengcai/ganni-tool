apiVersion: v1
kind: Service
metadata:
  labels:
    app: launcher-svc
    x: "1ww"
  name: launcher-svc
  namespace: launcher-test
spec:
  ports:
    - port: 81
      protocol: TCP
      targetPort: 80
      name: xxx
  selector:
    app: launcher